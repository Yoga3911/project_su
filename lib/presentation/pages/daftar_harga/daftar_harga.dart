import 'package:flutter/material.dart';

class DaftarHargaPage extends StatelessWidget {
  const DaftarHargaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Daftar Harga"),
      ),
    );
  }
}
